# Phase 1 Data Model: Site Scaffold & Deploy Pipeline

No data entities apply to this feature. The placeholder page has no forms,
persisted state, or content model — it is static markup — and the deploy
workflow has no data store. This file is intentionally minimal; a real data
model (e.g., a "Project" entity for the work gallery) will be introduced in
a future feature once real portfolio content is built.
